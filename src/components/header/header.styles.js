import styled from 'styled-components';

const StyledHeader = styled.header`
	margin-bottom: 24px;
	margin-top: 46px;
`;
const StyledH1 = styled.header`
	color: white;
	font-size: 1.75rem;
`;
const StyledText = styled.header`
	font-size: 0.875rem;
	color: #8b98c6;
`;

export { StyledHeader, StyledH1, StyledText };
